diff --git a/dist/core/agent-session.js b/dist/core/agent-session.js
index bd27f9c32c3ddfac52704f7544ae24a3a9e542e8..45f3e90906969827c0195554badd0f2c80c0ed53 100644
--- a/dist/core/agent-session.js
+++ b/dist/core/agent-session.js
@@ -176,12 +176,13 @@ export class AgentSession {
             }
             await this._agentEventQueue;
             try {
-                return await runner.emitToolCall({
+                const hookResult = await runner.emitToolCall({
                     type: "tool_call",
                     toolName: toolCall.name,
                     toolCallId: toolCall.id,
                     input: args,
                 });
+                return hookResult;
             }
             catch (err) {
                 if (err instanceof Error) {
diff --git a/dist/core/extensions/types.d.ts b/dist/core/extensions/types.d.ts
index 736c6550522fcffa71ca3e6d2f7cb5f13f5331f3..f0779b0dd0f5b1841b008fd822182a7c94fd72f2 100644
--- a/dist/core/extensions/types.d.ts
+++ b/dist/core/extensions/types.d.ts
@@ -704,6 +704,24 @@ export interface ToolCallEventResult {
     /** Block tool execution. To modify arguments, mutate `event.input` in place instead. */
     block?: boolean;
     reason?: string;
+    /**
+     * Optional request envelope surfaced in the synthesized tool result when the
+     * host wants to pause local execution and handle the tool externally.
+     */
+    request?: {
+        model?: string;
+        permission?: unknown;
+        metadata?: Record<string, unknown>;
+    };
+    /**
+     * Optional content/details override for immediate request-first fulfillment.
+     * When present, the runtime emits a synthesized tool result without running
+     * the local tool implementation.
+     */
+    content?: (TextContent | ImageContent)[];
+    details?: unknown;
+    isError?: boolean;
+    terminate?: boolean;
 }
 /** Result from user_bash event handler */
 export interface UserBashEventResult {
diff --git a/dist/core/tools/edit.js b/dist/core/tools/edit.js
index c0991daf4b1075a66f275bc086f6e9e966aa2aed..d7f49443b5058e67e05fc871b4657c4e22489996 100644
--- a/dist/core/tools/edit.js
+++ b/dist/core/tools/edit.js
@@ -39,6 +39,24 @@ function prepareEditArguments(input) {
         }
         catch { }
     }
+    // Some routed/switched models emit each edit as a stringified object
+    // (e.g. ["{\"oldText\":...}"]) rather than a real object. Parse those entries so
+    // the array validates as edits[] instead of failing with "edits.0: must be object".
+    if (Array.isArray(args.edits)) {
+        args.edits = args.edits.map((entry) => {
+            if (typeof entry !== "string")
+                return entry;
+            try {
+                const parsedEntry = JSON.parse(entry);
+                return parsedEntry && typeof parsedEntry === "object" && !Array.isArray(parsedEntry)
+                    ? parsedEntry
+                    : entry;
+            }
+            catch {
+                return entry;
+            }
+        });
+    }
     const legacy = args;
     if (typeof legacy.oldText !== "string" || typeof legacy.newText !== "string") {
         return args;
