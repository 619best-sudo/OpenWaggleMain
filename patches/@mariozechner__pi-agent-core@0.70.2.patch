diff --git a/dist/agent-loop.js b/dist/agent-loop.js
index 39ab91df1832ede1cdefd02a736f97b7e0b36dcc..f780f719639adb8bae59742afff6032830e68086 100644
--- a/dist/agent-loop.js
+++ b/dist/agent-loop.js
@@ -112,7 +112,7 @@ async function runLoop(currentContext, newMessages, config, signal, emit, stream
             const toolResults = [];
             hasMoreToolCalls = false;
             if (toolCalls.length > 0) {
-                const executedToolBatch = await executeToolCalls(currentContext, message, config, signal, emit);
+                const executedToolBatch = await executeToolCalls(currentContext, message, config, signal, emit, streamFn);
                 toolResults.push(...executedToolBatch.messages);
                 hasMoreToolCalls = !executedToolBatch.terminate;
                 for (const result of toolResults) {
@@ -156,57 +156,134 @@ async function streamAssistantResponse(context, config, signal, emit, streamFn)
     const streamFunction = streamFn || streamSimple;
     // Resolve API key (important for expiring tokens)
     const resolvedApiKey = (config.getApiKey ? await config.getApiKey(config.model.provider) : undefined) || config.apiKey;
-    const response = await streamFunction(config.model, llmContext, {
-        ...config,
-        apiKey: resolvedApiKey,
-        signal,
-    });
+    // Child abort controller. We use it to stop the orchestrator stream early —
+    // handing a routed-author tool (e.g. edit/write) to its model *before* the
+    // orchestrator generates the whole payload — without touching the run-level
+    // signal. It also mirrors real run aborts so cancellation still works.
+    let earlyAuthorCatch = null;
     let partialMessage = null;
     let addedPartial = false;
-    for await (const event of response) {
-        switch (event.type) {
-            case "start":
+    let response;
+    // Request-initiation retry. Under heavy load the backend/gateway can reject a
+    // request with a bodyless transient status (e.g. a spurious 401/5xx with no
+    // body) before any tokens stream. Those are not real auth/quota failures — a
+    // genuine bad key fails every request — so retry a bounded number of times
+    // with backoff. Only retried here, before streaming begins, so no partial
+    // assistant message exists yet and the retry cannot duplicate output.
+    let providerAbort;
+    let forwardAbort;
+    let createAttempt = 0;
+    while (true) {
+        providerAbort = new AbortController();
+        forwardAbort = () => providerAbort.abort();
+        if (signal) {
+            if (signal.aborted) {
+                providerAbort.abort();
+            }
+            else {
+                signal.addEventListener("abort", forwardAbort, { once: true });
+            }
+        }
+        try {
+            response = await streamFunction(config.model, llmContext, {
+                ...config,
+                apiKey: resolvedApiKey,
+                signal: providerAbort.signal,
+            });
+            break;
+        }
+        catch (error) {
+            if (signal) {
+                signal.removeEventListener("abort", forwardAbort);
+            }
+            if (!(signal && signal.aborted) &&
+                createAttempt < MAX_TRANSIENT_STREAM_RETRIES &&
+                isTransientStreamError(error)) {
+                createAttempt += 1;
+                await new Promise((resolve) => setTimeout(resolve, TRANSIENT_STREAM_RETRY_BASE_MS * createAttempt));
+                continue;
+            }
+            throw error;
+        }
+    }
+    try {
+        for await (const event of response) {
+            if (event.type === "start") {
                 partialMessage = event.partial;
                 context.messages.push(partialMessage);
                 addedPartial = true;
                 await emit({ type: "message_start", message: { ...partialMessage } });
-                break;
-            case "text_start":
-            case "text_delta":
-            case "text_end":
-            case "thinking_start":
-            case "thinking_delta":
-            case "thinking_end":
-            case "toolcall_start":
-            case "toolcall_delta":
-            case "toolcall_end":
-                if (partialMessage) {
-                    partialMessage = event.partial;
-                    context.messages[context.messages.length - 1] = partialMessage;
-                    await emit({
-                        type: "message_update",
-                        assistantMessageEvent: event,
-                        message: { ...partialMessage },
-                    });
-                }
-                break;
-            case "done":
-            case "error": {
+                continue;
+            }
+            if (event.type === "done" || event.type === "error") {
                 const finalMessage = await response.result();
                 if (addedPartial) {
                     context.messages[context.messages.length - 1] = finalMessage;
                 }
                 else {
                     context.messages.push(finalMessage);
-                }
-                if (!addedPartial) {
                     await emit({ type: "message_start", message: { ...finalMessage } });
                 }
                 await emit({ type: "message_end", message: finalMessage });
                 return finalMessage;
             }
+            if (partialMessage) {
+                partialMessage = event.partial;
+                context.messages[context.messages.length - 1] = partialMessage;
+                await emit({
+                    type: "message_update",
+                    assistantMessageEvent: event,
+                    message: { ...partialMessage },
+                });
+            }
+            if (partialMessage &&
+                (event.type === "toolcall_start" ||
+                    event.type === "toolcall_delta" ||
+                    event.type === "toolcall_end")) {
+                earlyAuthorCatch = detectEarlyAuthorCatch(partialMessage);
+                if (earlyAuthorCatch) {
+                    providerAbort.abort();
+                    break;
+                }
+            }
+        }
+    }
+    catch (error) {
+        // An intentional early-author abort surfaces here as a stream error; only
+        // rethrow genuine failures.
+        if (!earlyAuthorCatch) {
+            throw error;
+        }
+    }
+    finally {
+        if (signal) {
+            signal.removeEventListener("abort", forwardAbort);
         }
     }
+    if (earlyAuthorCatch) {
+        // Swallow any late rejection from the aborted provider stream.
+        if (response && typeof response.result === "function") {
+            try {
+                const pending = response.result();
+                if (pending && typeof pending.then === "function") {
+                    pending.then(() => { }, () => { });
+                }
+            }
+            catch {
+                // ignore
+            }
+        }
+        const finalMessage = buildEarlyAuthorAssistantMessage(partialMessage, earlyAuthorCatch);
+        if (addedPartial) {
+            context.messages[context.messages.length - 1] = finalMessage;
+        }
+        else {
+            context.messages.push(finalMessage);
+            await emit({ type: "message_start", message: { ...finalMessage } });
+        }
+        await emit({ type: "message_end", message: finalMessage });
+        return finalMessage;
+    }
     const finalMessage = await response.result();
     if (addedPartial) {
         context.messages[context.messages.length - 1] = finalMessage;
@@ -218,18 +295,127 @@ async function streamAssistantResponse(context, config, signal, emit, streamFn)
     await emit({ type: "message_end", message: finalMessage });
     return finalMessage;
 }
+const MAX_TRANSIENT_STREAM_RETRIES = 2;
+const TRANSIENT_STREAM_RETRY_BASE_MS = 400;
+/**
+ * A bodyless status error is a transport/proxy hiccup (gateway overload, dropped
+ * connection), not an actionable auth/quota/context failure — those always carry
+ * a body. Retrying the request initiation for these recovers from transient
+ * backend rejections under load.
+ */
+function isTransientStreamError(error) {
+    const message = (error && typeof error.message === "string" ? error.message : String(error)).toLowerCase();
+    if (!message.includes("no body")) {
+        return false;
+    }
+    return ["401", "403", "500", "502", "503", "504", "429"].some((code) => message.includes(code));
+}
+/**
+ * Read the host-provided early-authoring plan for a tool. The host (OpenWaggle)
+ * registers a pure lookup on `globalThis.__openwaggleEarlyToolAuthoring` that maps
+ * a tool name to `{ targetKeys, payloadKeys }` when that tool's route wants the
+ * routed model to author the final arguments. Returns null when not applicable.
+ */
+function resolveEarlyAuthoringPlan(toolName) {
+    const resolver = globalThis.__openwaggleEarlyToolAuthoring;
+    if (typeof resolver !== "function") {
+        return null;
+    }
+    try {
+        const plan = resolver(toolName);
+        if (plan && Array.isArray(plan.targetKeys) && Array.isArray(plan.payloadKeys)) {
+            return plan;
+        }
+    }
+    catch {
+        // ignore resolver errors — fall back to post-execution interception
+    }
+    return null;
+}
+/**
+ * Detect whether the in-progress assistant message contains a routed-author tool
+ * call whose target (e.g. `path`) is known but whose payload (e.g. `content`,
+ * `edits`) has only just begun — the point at which we can cut the orchestrator
+ * off and let the routed model author the payload. Returns the caught tool call
+ * (with payload keys stripped to leave a clean intent) or null.
+ */
+function detectEarlyAuthorCatch(partialMessage) {
+    const content = partialMessage?.content;
+    if (!Array.isArray(content)) {
+        return null;
+    }
+    for (const block of content) {
+        if (!block || block.type !== "toolCall") {
+            continue;
+        }
+        const name = typeof block.name === "string" ? block.name : "";
+        const id = typeof block.id === "string" ? block.id : "";
+        if (!name || !id) {
+            continue;
+        }
+        const plan = resolveEarlyAuthoringPlan(name);
+        if (!plan) {
+            continue;
+        }
+        const args = isPlainObject(block.arguments) ? block.arguments : null;
+        if (!args) {
+            continue;
+        }
+        const hasTarget = plan.targetKeys.some((key) => typeof args[key] === "string" && args[key].trim().length > 0);
+        // A payload key having appeared implies the target value is complete (a
+        // later key started), which guards against cutting on a truncated target.
+        const hasPayload = plan.payloadKeys.some((key) => Object.hasOwn(args, key));
+        if (hasTarget && hasPayload) {
+            const intentArgs = {};
+            for (const [key, value] of Object.entries(args)) {
+                if (!plan.payloadKeys.includes(key)) {
+                    intentArgs[key] = value;
+                }
+            }
+            return { toolCallId: id, toolName: name, intentArgs };
+        }
+    }
+    return null;
+}
+function buildEarlyAuthorAssistantMessage(partialMessage, caught) {
+    const sourceContent = Array.isArray(partialMessage?.content) ? partialMessage.content : [];
+    const content = [];
+    let placed = false;
+    for (const block of sourceContent) {
+        if (block && block.type === "toolCall") {
+            if (block.id === caught.toolCallId && !placed) {
+                const { partialArgs: _partialArgs, streamIndex: _streamIndex, ...rest } = block;
+                content.push({ ...rest, arguments: caught.intentArgs });
+                placed = true;
+            }
+            // Drop other / still-incomplete tool calls.
+            continue;
+        }
+        content.push(block);
+    }
+    if (!placed) {
+        content.push({
+            type: "toolCall",
+            id: caught.toolCallId,
+            name: caught.toolName,
+            arguments: caught.intentArgs,
+        });
+    }
+    const base = isPlainObject(partialMessage) ? partialMessage : { role: "assistant" };
+    return { ...base, role: "assistant", content, stopReason: "toolUse" };
+}
 /**
  * Execute tool calls from an assistant message.
  */
-async function executeToolCalls(currentContext, assistantMessage, config, signal, emit) {
+async function executeToolCalls(currentContext, assistantMessage, config, signal, emit, streamFn) {
     const toolCalls = assistantMessage.content.filter((c) => c.type === "toolCall");
     const hasSequentialToolCall = toolCalls.some((tc) => currentContext.tools?.find((t) => t.name === tc.name)?.executionMode === "sequential");
     if (config.toolExecution === "sequential" || hasSequentialToolCall) {
-        return executeToolCallsSequential(currentContext, assistantMessage, toolCalls, config, signal, emit);
+        return executeToolCallsSequential(currentContext, assistantMessage, toolCalls, config, signal, emit, streamFn);
     }
-    return executeToolCallsParallel(currentContext, assistantMessage, toolCalls, config, signal, emit);
+    return executeToolCallsParallel(currentContext, assistantMessage, toolCalls, config, signal, emit, streamFn);
 }
-async function executeToolCallsSequential(currentContext, assistantMessage, toolCalls, config, signal, emit) {
+async function executeToolCallsSequential(currentContext, assistantMessage, toolCalls, config, signal, emit, streamFn) {
     const finalizedCalls = [];
     const messages = [];
     for (const toolCall of toolCalls) {
@@ -239,7 +425,7 @@ async function executeToolCallsSequential(currentContext, assistantMessage, tool
             toolName: toolCall.name,
             args: toolCall.arguments,
         });
-        const preparation = await prepareToolCall(currentContext, assistantMessage, toolCall, config, signal);
+        const preparation = await prepareToolCall(currentContext, assistantMessage, toolCall, config, signal, streamFn);
         let finalized;
         if (preparation.kind === "immediate") {
             finalized = {
@@ -251,6 +437,15 @@ async function executeToolCallsSequential(currentContext, assistantMessage, tool
         else {
             const executed = await executePreparedToolCall(preparation, signal, emit);
             finalized = await finalizeExecutedToolCall(currentContext, assistantMessage, preparation, executed, config, signal);
+            finalized = await applyRoutedResultReasoning({
+                finalized,
+                preparation,
+                assistantMessage,
+                currentContext,
+                config,
+                signal,
+                streamFn,
+            });
         }
         await emitToolExecutionEnd(finalized, emit);
         const toolResultMessage = createToolResultMessage(finalized);
@@ -263,7 +458,7 @@ async function executeToolCallsSequential(currentContext, assistantMessage, tool
         terminate: shouldTerminateToolBatch(finalizedCalls),
     };
 }
-async function executeToolCallsParallel(currentContext, assistantMessage, toolCalls, config, signal, emit) {
+async function executeToolCallsParallel(currentContext, assistantMessage, toolCalls, config, signal, emit, streamFn) {
     const finalizedCalls = [];
     for (const toolCall of toolCalls) {
         await emit({
@@ -272,7 +467,7 @@ async function executeToolCallsParallel(currentContext, assistantMessage, toolCa
             toolName: toolCall.name,
             args: toolCall.arguments,
         });
-        const preparation = await prepareToolCall(currentContext, assistantMessage, toolCall, config, signal);
+        const preparation = await prepareToolCall(currentContext, assistantMessage, toolCall, config, signal, streamFn);
         if (preparation.kind === "immediate") {
             const finalized = {
                 toolCall,
@@ -285,7 +480,16 @@ async function executeToolCallsParallel(currentContext, assistantMessage, toolCa
         }
         finalizedCalls.push(async () => {
             const executed = await executePreparedToolCall(preparation, signal, emit);
-            const finalized = await finalizeExecutedToolCall(currentContext, assistantMessage, preparation, executed, config, signal);
+            let finalized = await finalizeExecutedToolCall(currentContext, assistantMessage, preparation, executed, config, signal);
+            finalized = await applyRoutedResultReasoning({
+                finalized,
+                preparation,
+                assistantMessage,
+                currentContext,
+                config,
+                signal,
+                streamFn,
+            });
             await emitToolExecutionEnd(finalized, emit);
             return finalized;
         });
@@ -318,7 +522,7 @@ function prepareToolCallArguments(tool, toolCall) {
         arguments: preparedArguments,
     };
 }
-async function prepareToolCall(currentContext, assistantMessage, toolCall, config, signal) {
+async function prepareToolCall(currentContext, assistantMessage, toolCall, config, signal, streamFn) {
     const tool = currentContext.tools?.find((t) => t.name === toolCall.name);
     if (!tool) {
         return {
@@ -329,7 +533,27 @@ async function prepareToolCall(currentContext, assistantMessage, toolCall, confi
     }
     try {
         const preparedToolCall = prepareToolCallArguments(tool, toolCall);
-        const validatedArgs = validateToolArguments(tool, preparedToolCall);
+        // Routed-author tools can arrive with partial arguments (the orchestrator
+        // was cut off early, before generating the payload). Validate leniently for
+        // them: keep the proposed args as intent and let the routed model author the
+        // final, valid arguments. Non-author tools validate strictly as before.
+        const authorTool = resolveEarlyAuthoringPlan(tool.name) !== null;
+        let validatedArgs;
+        let proposedValidationError = null;
+        if (authorTool) {
+            try {
+                validatedArgs = validateToolArguments(tool, preparedToolCall);
+            }
+            catch (error) {
+                validatedArgs = preparedToolCall.arguments;
+                proposedValidationError = error;
+            }
+        }
+        else {
+            validatedArgs = validateToolArguments(tool, preparedToolCall);
+        }
+        let routeDirective = null;
+        let routedAuthored = false;
         if (config.beforeToolCall) {
             const beforeResult = await config.beforeToolCall({
                 assistantMessage,
@@ -344,12 +568,48 @@ async function prepareToolCall(currentContext, assistantMessage, toolCall, confi
                     isError: true,
                 };
             }
+            if (shouldImmediateResolveToolCall(beforeResult)) {
+                return {
+                    kind: "immediate",
+                    result: createBeforeToolCallImmediateResult(toolCall, validatedArgs, beforeResult),
+                    isError: beforeResult?.isError ?? false,
+                };
+            }
+            // Capture the routing directive so post-execution phases (e.g. routed
+            // reasoning over the result) can act on it after the tool runs.
+            routeDirective = beforeResult?.route ?? null;
+            // Routed authoring: the host routed this tool to another model and asked
+            // that model to author the final, executable arguments. This is the
+            // ownership model for mutation tools — the orchestrator only proposed an
+            // intent; the routed model produces the payload that actually runs. The
+            // tool then executes locally with those arguments, so permission and
+            // skip-permission paths converge on the same routed execution.
+            if (shouldAuthorWithRoutedModel(beforeResult)) {
+                validatedArgs = await authorRoutedToolArguments({
+                    currentContext,
+                    assistantMessage,
+                    tool,
+                    toolCall,
+                    proposedArgs: validatedArgs,
+                    routedModel: beforeResult.route.model,
+                    config,
+                    signal,
+                    streamFn,
+                });
+                routedAuthored = true;
+            }
+        }
+        // If we never routed-authored and the orchestrator's arguments failed
+        // validation, surface that error now (matches the pre-lenient behavior).
+        if (!routedAuthored && proposedValidationError) {
+            throw proposedValidationError;
         }
         return {
             kind: "prepared",
             toolCall,
             tool,
             args: validatedArgs,
+            route: routeDirective,
         };
     }
     catch (error) {
@@ -360,6 +620,255 @@ async function prepareToolCall(currentContext, assistantMessage, toolCall, confi
         };
     }
 }
+function shouldAuthorWithRoutedModel(beforeResult) {
+    return Boolean(beforeResult &&
+        !beforeResult.request &&
+        beforeResult.route &&
+        typeof beforeResult.route.model === "string" &&
+        beforeResult.route.model.trim().length > 0 &&
+        beforeResult.route.authorFinalArgs === true);
+}
+/**
+ * Run a forced, single-tool completion on the routed model so it authors the
+ * final arguments for `tool`. The routed model shares the orchestrator's provider
+ * gateway, so it is reached by cloning the loop model with the routed id — the
+ * routed model is passed explicitly, never inferred from a prompt.
+ *
+ * Returns the routed model's validated arguments. On any failure it falls back to
+ * the orchestrator's proposed arguments so a routing hiccup degrades to today's
+ * behavior instead of failing the tool call.
+ */
+async function authorRoutedToolArguments(input) {
+    const { currentContext, assistantMessage, tool, toolCall, proposedArgs, routedModel, config, signal, streamFn, } = input;
+    try {
+        const routedModelConfig = { ...config.model, id: routedModel };
+        // Exclude the pending assistant tool-call message: it carries an unfulfilled
+        // tool call that would make the fresh completion an invalid conversation.
+        const historyMessages = currentContext.messages.filter((message) => message !== assistantMessage);
+        const llmMessages = config.convertToLlm
+            ? await config.convertToLlm(historyMessages)
+            : historyMessages;
+        const authoringInstruction = {
+            role: "user",
+            content: [
+                {
+                    type: "text",
+                    text: [
+                        `The planner selected the \`${tool.name}\` tool for the current step.`,
+                        `You are the specialist model responsible for authoring the FINAL, correct arguments for this single tool call.`,
+                        ``,
+                        `Planner's proposed arguments (treat as intent, not as final — correct and complete them using the full context):`,
+                        JSON.stringify(proposedArgs, null, 2),
+                        ``,
+                        `Call \`${tool.name}\` exactly once with the final arguments. Do not call any other tool and do not produce any other output.`,
+                        `Provide only the arguments defined by the tool's schema — do not add extra fields such as "description". Structured values (arrays and objects) must be emitted as real JSON arrays/objects, never as stringified JSON.`,
+                    ].join("\n"),
+                },
+            ],
+        };
+        const authoringSystemPrompt = [
+            currentContext.systemPrompt ?? "",
+            `You are authoring the final arguments for a single \`${tool.name}\` tool call. Emit exactly one call to \`${tool.name}\`.`,
+        ]
+            .filter(Boolean)
+            .join("\n\n");
+        const llmContext = {
+            systemPrompt: authoringSystemPrompt,
+            messages: [...llmMessages, authoringInstruction],
+            tools: [tool],
+        };
+        const resolvedApiKey = (config.getApiKey ? await config.getApiKey(routedModelConfig.provider) : undefined) ||
+            config.apiKey;
+        const streamFunction = streamFn || streamSimple;
+        const response = await streamFunction(routedModelConfig, llmContext, {
+            ...config,
+            apiKey: resolvedApiKey,
+            signal,
+            toolChoice: { type: "function", function: { name: tool.name } },
+        });
+        // Drain the stream so `result()` resolves with the final message.
+        for await (const _event of response) {
+            void _event;
+        }
+        const finalMessage = await response.result();
+        const authoredCall = finalMessage.content.find((block) => block.type === "toolCall" && block.name === tool.name);
+        if (!authoredCall) {
+            return proposedArgs;
+        }
+        // Normalize the routed model's output exactly like a first-class tool call:
+        // run the tool's own `prepareArguments` (e.g. the edit tool parses `edits`
+        // sent as a JSON string), then drop any keys the strict schema forbids
+        // (routed models sometimes add stray fields like `description`). Without
+        // this, valid-but-loosely-shaped output would fail validation and the tool
+        // would silently fall back to the orchestrator's arguments.
+        const preparedAuthoredCall = prepareToolCallArguments(tool, authoredCall);
+        const sanitizedArgs = stripSchemaUnknownRootKeys(tool, preparedAuthoredCall.arguments);
+        return validateToolArguments(tool, { ...preparedAuthoredCall, arguments: sanitizedArgs });
+    }
+    catch (error) {
+        if (signal?.aborted) {
+            throw error;
+        }
+        // Degrade gracefully to the orchestrator's proposed arguments.
+        return proposedArgs;
+    }
+}
+/**
+ * Drop top-level keys the tool schema does not allow. Only applies when the
+ * schema is a closed object (`additionalProperties: false`) with declared
+ * `properties`; otherwise the arguments are returned unchanged.
+ */
+function stripSchemaUnknownRootKeys(tool, args) {
+    const schema = tool?.parameters;
+    if (!isPlainObject(args) || !isPlainObject(schema)) {
+        return args;
+    }
+    if (schema.additionalProperties !== false || !isPlainObject(schema.properties)) {
+        return args;
+    }
+    const allowedKeys = new Set(Object.keys(schema.properties));
+    let removedKey = false;
+    const result = {};
+    for (const [key, value] of Object.entries(args)) {
+        if (allowedKeys.has(key)) {
+            result[key] = value;
+        }
+        else {
+            removedKey = true;
+        }
+    }
+    return removedKey ? result : args;
+}
+function shouldReasonOverRoutedResult(route) {
+    return Boolean(route &&
+        route.reasonOverResult === true &&
+        typeof route.model === "string" &&
+        route.model.trim().length > 0);
+}
+function extractTextFromContentBlocks(content) {
+    if (typeof content === "string") {
+        return content;
+    }
+    if (!Array.isArray(content)) {
+        return "";
+    }
+    return content
+        .map((block) => (isPlainObject(block) && block.type === "text" && typeof block.text === "string" ? block.text : ""))
+        .filter(Boolean)
+        .join("\n");
+}
+function describeRoutedToolTarget(toolCall) {
+    const args = toolCall?.arguments;
+    if (!isPlainObject(args)) {
+        return "";
+    }
+    const path = typeof args.path === "string" ? args.path : typeof args.filePath === "string" ? args.filePath : "";
+    return path;
+}
+/**
+ * Post-execution routed reasoning. When a route requests `reasonOverResult`, run
+ * the routed model over the (already executed) tool result and append its
+ * analysis to the result the orchestrator sees. The tool itself still executed
+ * with the orchestrator's arguments — only the reading/reasoning is delegated.
+ * Any failure degrades to the unaugmented result.
+ */
+async function applyRoutedResultReasoning(input) {
+    const { finalized, preparation, assistantMessage, currentContext, config, signal, streamFn } = input;
+    const route = preparation?.route;
+    if (!shouldReasonOverRoutedResult(route) || finalized.isError) {
+        return finalized;
+    }
+    try {
+        const reasoning = await reasonOverRoutedToolResult({
+            currentContext,
+            assistantMessage,
+            tool: preparation.tool,
+            toolCall: preparation.toolCall,
+            result: finalized.result,
+            routedModel: route.model,
+            config,
+            signal,
+            streamFn,
+        });
+        if (!reasoning) {
+            return finalized;
+        }
+        const existingContent = Array.isArray(finalized.result?.content) ? finalized.result.content : [];
+        return {
+            ...finalized,
+            result: {
+                ...finalized.result,
+                content: [...existingContent, { type: "text", text: reasoning }],
+            },
+        };
+    }
+    catch (error) {
+        if (signal?.aborted) {
+            throw error;
+        }
+        // Degrade gracefully to the unaugmented tool result.
+        return finalized;
+    }
+}
+async function reasonOverRoutedToolResult(input) {
+    const { currentContext, assistantMessage, tool, toolCall, result, routedModel, config, signal, streamFn } = input;
+    const resultText = extractTextFromContentBlocks(result?.content);
+    if (!resultText.trim()) {
+        return null;
+    }
+    const routedModelConfig = { ...config.model, id: routedModel };
+    // Exclude the pending assistant tool-call message: its tool call has no
+    // matching tool result yet, which would make a fresh completion invalid.
+    const historyMessages = currentContext.messages.filter((message) => message !== assistantMessage);
+    const llmMessages = config.convertToLlm
+        ? await config.convertToLlm(historyMessages)
+        : historyMessages;
+    const target = describeRoutedToolTarget(toolCall);
+    const reasoningInstruction = {
+        role: "user",
+        content: [
+            {
+                type: "text",
+                text: [
+                    `The \`${tool.name}\` tool was executed${target ? ` for ${target}` : ""}.`,
+                    `Read the result below and reason about it for the current task. Produce a concise, focused analysis: what the content contains and the parts most relevant to the current task. Respond with text only; do not call any tool.`,
+                    ``,
+                    `--- ${tool.name} result ---`,
+                    resultText,
+                ].join("\n"),
+            },
+        ],
+    };
+    const reasoningSystemPrompt = [
+        currentContext.systemPrompt ?? "",
+        `You are a specialist reader. Analyze the provided tool result and return a concise reasoning summary. Do not call tools; respond with text only.`,
+    ]
+        .filter(Boolean)
+        .join("\n\n");
+    const llmContext = {
+        systemPrompt: reasoningSystemPrompt,
+        messages: [...llmMessages, reasoningInstruction],
+        tools: [],
+    };
+    const resolvedApiKey = (config.getApiKey ? await config.getApiKey(routedModelConfig.provider) : undefined) ||
+        config.apiKey;
+    const streamFunction = streamFn || streamSimple;
+    const response = await streamFunction(routedModelConfig, llmContext, {
+        ...config,
+        apiKey: resolvedApiKey,
+        signal,
+    });
+    // Drain the stream so `result()` resolves with the final message.
+    for await (const _event of response) {
+        void _event;
+    }
+    const finalMessage = await response.result();
+    const text = extractTextFromContentBlocks(finalMessage?.content).trim();
+    if (!text) {
+        return null;
+    }
+    return `Analysis by ${routedModel}:\n${text}`;
+}
 async function executePreparedToolCall(prepared, signal, emit) {
     const updateEvents = [];
     try {
@@ -422,6 +931,39 @@ function createErrorToolResult(message) {
         details: {},
     };
 }
+function shouldImmediateResolveToolCall(beforeResult) {
+    return Boolean(beforeResult &&
+        (beforeResult.request ||
+            beforeResult.content !== undefined ||
+            beforeResult.details !== undefined ||
+            beforeResult.isError !== undefined ||
+            beforeResult.terminate !== undefined));
+}
+function createBeforeToolCallImmediateResult(toolCall, args, beforeResult) {
+    const requestDetails = beforeResult?.request
+        ? {
+            kind: "tool_request",
+            toolName: toolCall.name,
+            args,
+            request: beforeResult.request,
+        }
+        : undefined;
+    const customDetails = beforeResult?.details;
+    const mergedDetails = requestDetails && isPlainObject(customDetails)
+        ? { ...requestDetails, ...customDetails }
+        : customDetails ?? requestDetails ?? {};
+    const defaultText = beforeResult?.request
+        ? `Tool execution requested for ${toolCall.name}${beforeResult.request.model ? ` using model ${beforeResult.request.model}` : ""}.`
+        : beforeResult?.reason || `Tool execution was handled before local execution for ${toolCall.name}.`;
+    return {
+        content: beforeResult?.content ?? [{ type: "text", text: defaultText }],
+        details: mergedDetails,
+        terminate: beforeResult?.terminate ?? Boolean(beforeResult?.request),
+    };
+}
+function isPlainObject(value) {
+    return typeof value === "object" && value !== null && !Array.isArray(value);
+}
 async function emitToolExecutionEnd(finalized, emit) {
     await emit({
         type: "tool_execution_end",
diff --git a/dist/types.d.ts b/dist/types.d.ts
index af4599795035cdb76758e0d560d3c69970ce2670..3452d4bbe823e1ee5715ff37c4b58342bf17f83c 100644
--- a/dist/types.d.ts
+++ b/dist/types.d.ts
@@ -23,15 +23,76 @@ export type ToolExecutionMode = "sequential" | "parallel";
 export type AgentToolCall = Extract<AssistantMessage["content"][number], {
     type: "toolCall";
 }>;
+export interface BeforeToolCallRequest {
+    /**
+     * Optional executor model hint chosen by the host/router for this tool request.
+     * The core runtime does not execute the model switch itself; it surfaces the
+     * request in the synthesized tool result for the host to act on.
+     */
+    model?: string;
+    /**
+     * Optional permission payload that the host can use to ask the user for
+     * approval before actually executing the tool.
+     */
+    permission?: unknown;
+    /**
+     * Arbitrary host metadata preserved in the synthesized tool result details.
+     */
+    metadata?: Record<string, unknown>;
+}
 /**
  * Result returned from `beforeToolCall`.
  *
  * Returning `{ block: true }` prevents the tool from executing. The loop emits an error tool result instead.
  * `reason` becomes the text shown in that error result. If omitted, a default blocked message is used.
+ *
+ * Returning `request`, `content`, or `details` short-circuits local execution and
+ * emits a synthesized tool result immediately. This is intended for request-first
+ * runtimes that want to surface a tool proposal to the host for permission,
+ * delegation, or external fulfillment before continuing.
  */
+export interface BeforeToolCallRoute {
+    /**
+     * The model responsible for this tool call. When `authorFinalArgs` is true the
+     * runtime runs a forced single-tool completion on this model to author the
+     * final, executable arguments before the tool runs.
+     */
+    model: string;
+    /**
+     * When true, the routed model authors the final tool arguments (the runtime
+     * ignores the orchestrator's proposed arguments as anything more than intent).
+     * When false/absent the route is informational and the tool executes with the
+     * orchestrator's arguments.
+     */
+    authorFinalArgs?: boolean;
+    /**
+     * When true, the routed model reasons over the tool result after execution and
+     * its analysis is appended to the result the caller sees. The tool still
+     * executes with the orchestrator's arguments — only the reading/reasoning is
+     * delegated.
+     */
+    reasonOverResult?: boolean;
+}
 export interface BeforeToolCallResult {
     block?: boolean;
     reason?: string;
+    request?: BeforeToolCallRequest;
+    /**
+     * Routing directive. When present without `request`, and `authorFinalArgs` is
+     * true, the runtime has the routed model re-author the final arguments and then
+     * executes the tool locally. This lets permission and skip-permission paths
+     * converge on the same routed execution logic.
+     */
+    route?: BeforeToolCallRoute;
+    content?: (TextContent | ImageContent)[];
+    details?: unknown;
+    isError?: boolean;
+    /**
+     * Hint that the agent should stop after the current tool batch.
+     * Defaults to `true` when `request` is provided so hosts can pause on
+     * permission/delegation boundaries.
+     */
+    terminate?: boolean;
 }
 /**
  * Partial override returned from `afterToolCall`.
